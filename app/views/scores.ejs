<%- include('partials/header') %>
<div class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center">
	<h2 class="h1 display-4">Vous avez fait <%= scores %> bonnes réponses</h2>
	<div class="row">
		<% for (const [index, element] of result.questions.entries()){%>
		<div class="col-12">
			<label class="badge level level--<%=element.level.name.toLowerCase()%>">
				<%=element.level.name%>
			</label>
			<h4><%=element.question%></h4>
			<ol>
				<% for (const [i, elem] of element.answers.entries()){%>
				<%if(element.answer_id === elem.id) {%>
				<li style="color: #28a745"><%=elem.description%></li>
				<% } else {%>
				<li style="color: #e33545"><%=elem.description%></li>
				<% } %> <% } %>
			</ol>
		</div>
		<%}%>
	</div>
</div>
<%- include('partials/footer') %>
